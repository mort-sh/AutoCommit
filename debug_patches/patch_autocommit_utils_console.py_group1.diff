diff --git a/autocommit/utils/console.py b/autocommit/utils/console.py
index 7444423..8585443 100644
--- a/autocommit/utils/console.py
+++ b/autocommit/utils/console.py
\n@@ -299,6 +299,9 @@ def render_final_summary(
     repo_path: str,
     committed_files: list[str],
     push_status: Literal["pushed", "failed", "skipped", "not_attempted"],
+    total_files: int,
+    total_lines_changed: int,
+    config: Any # Use Any to avoid circular import
 ):
     """Renders the final summary after commits and optional push."""
     term_width = console.width
\n@@ -371,15 +374,28 @@ def render_final_summary(
 
         console.print(tree)
 
-    # --- Push Status ---
+    # --- Push Status Section ---
     console.print("\n--- Push Status ---", style="bold")
     if push_status == "pushed":
-        console.print(f"{STATUS_OK_ICON} Changes pushed successfully.", style="success")
+        console.print(f"{STATUS_OK_ICON}  Changes successfully pushed.", style="success")
     elif push_status == "failed":
-        console.print(f"{ERROR_ICON} Push failed. Check logs above.", style="error")
+        console.print(f"{ERROR_ICON}  Push operation failed. Check logs above.", style="warning")
     elif push_status == "skipped":
-        console.print(f"{INFO_ICON} Push skipped (Test Mode or no commits).", style="info")
+        console.print(f"{INFO_ICON}  Push skipped (test mode or no commits).", style="info")
     elif push_status == "not_attempted":
-        console.print(f"{INFO_ICON} Push not requested.", style="info")
-
-    console.print("\n") # Final newline
+        console.print(f"{INFO_ICON}  Push not requested.", style="info")
+
+    # --- Overall Stats Section ---
+    console.print("\n\nSummary: ", style="bold")
+    total_committed = len(committed_files)
+    # Display total files tracked/staged vs committed
+    console.print(f"\t- {total_committed}/{total_files} files committed")
+    # Display total lines changed
+    console.print(f"\t- {total_lines_changed} lines changed (approx.)") # Note: This is total lines, not just committed
+    # Display config settings used
+    console.print(f"\t- Chunk level: {config.chunk_level} ({config.get_chunk_level_description()})")
+    parallel_desc = f"{config.parallel} workers" if config.parallel > 0 else "auto"
+    console.print(f"\t- Parallelism: {parallel_desc}")
+    console.print(f"\t- Model: {config.model}")
+
+    console.print("\n") # Add final newline
\n